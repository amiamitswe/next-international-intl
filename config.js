import { Pathnames } from "next-intl/navigation";

export const defaultLocale = "en";
export const locales = ["en", "de", "bd"];
export const localePrefix = undefined;

export const pathnames = {
  "/": "/",
  "/pathnames": {
    en: "/pathnames",
    de: "/pfadnamen",
  },
};
